import 'package:flutter/material.dart';

class Samsung extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Apple Screen'),
      ),
    );
  }
}